name: Gitleaks

on:
  pull_request:
  workflow_dispatch:
  schedule:
    - cron: "0 0 * * *"

permissions:
  contents: read
  pull-requests: write

jobs:
  gitleaks:
    uses: Diaszano/actions/.github/workflows/gitleaks.yaml@v1.0.0
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}
